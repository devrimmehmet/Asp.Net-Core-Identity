@model SignInViewModel
@section Scripts{
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
@{
    ViewData["Title"] = "SignIn";
    var SuccessMessage = TempData["SuccessMessage"]; // TempData'dan veriyi almak
}


<div class="row">
    <div class="col-md-6 offset-md-3">
        <h2>Kullanıcı Giriş</h2>

        @if (@SuccessMessage != null)
        {
            <div class="alert alert-info" role="alert">
                @SuccessMessage
            </div>

        }
        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

        <form asp-controller="Home" asp-action="SignIn" asp-route-returnUrl="@Context.Request.Query["returnUrl"]" method="post">
            <div class="row mb-3">

                <div class="col-md-8">
                    <label for="Email" class="col-md-2 col-form-label" asp-for="Email"></label>
                    <input asp-for="Email" value="devrimmehmet@gmail.com" class="form-control">
                    <span class="text-danger" asp-validation-for="Email"></span>
                </div>
            </div>
            <div class="row mb-3">

                <div class="col-md-8">
                    <label for="Password" class="col-md-2 col-form-label" asp-for="Password"></label>
                    <input type="password" asp-for="Password" value="37Password-*1923" class="form-control">
                    <span class="text-danger" asp-validation-for="Password"></span>
                </div>
            </div>
<div class="row">
                <div class="col-md-4 ">
                    <div class="mb-4 form-check">
                        <input class="form-check-input" asp-for="RememberMe" checked>
                        <label class="form-check-label" asp-for="RememberMe"></label>
                    </div>
                </div>
                <div class="col-md-4 ">
                    <a asp-controller="Home" asp-action="ForgetPassword" class="btn btn-outline-secondary btn-sm float-end">Şifremi Unuttum</a>
                </div>
            </div>
                <row>
            <button type="submit" class="btn btn-primary">Giriş Yap</button>
            </row>

        </form>
    </div>
</div>




